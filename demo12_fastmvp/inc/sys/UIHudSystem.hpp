#pragma once
#include "util/Draw.hpp"

class UIHudSystem
{

    private:
        static constexpr uint8_t NUM_INVENTORY_SLOTS = 4;
};


//draw.drawText(UI_LOG_X + 2, y, s.text.c_str(), Color( alpha | rgb));
